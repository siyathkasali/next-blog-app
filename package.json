{
  "name": "Next Web Application",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ENV_FILE=.env.local NODE_ENV=development node server.js",
    "production": "ENV_FILE=.env.production NODE_ENV=production && npm run build",
    "build": "next build && next export",
    "start": "next start"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "next": "^10.0.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "sass": "^1.30.0"
  }
}
